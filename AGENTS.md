<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Public navigation and verification

- `/ar/notable-figures` and `/en/notable-figures` are the unified «شخصيات بارزة» / “Notable figures” listings, including all rulers and biographies. Keep article URLs and stored section slugs unchanged; the `/people` and `/rulers` listings in both languages permanently redirect to the unified listings with query parameters preserved.
- `src/lib/site.ts` is imported by the client-side header. Keep its encyclopedia import type-only: a runtime import pulls the introductory article dataset into browser JavaScript. `publicNavigation()` maps CMS sections to header/footer/search links and merges biography aliases; the server layout passes only labels and paths to the header. The homepage exposes additional CMS sections without adding them to seed content. Unit tests check navigation labels, dynamic sections and safe slugs.
- Keep masthead labels compact via `navigationLabel()` (المناطق، الشخصيات، الاقتصاد، الطبيعة، السياحة / Regions, People, Economy, Nature, Tourism). Full section names remain in CMS, headings, search and footer; masthead link titles expose the full names. Do not rename section slugs or CMS records just to shorten the navigation.
- Public article decoding validates section slug syntax, not a fixed four-section enum. Article workflow hooks validate section existence against the CMS, and section deletion guards protect references. Keep bilingual pairing, approval and public-media requirements intact when adding sections.
- The homepage «شخصيات بارزة» cards show only available `kind=ruler` entries (currently all seven kings), never a filler artist or other notable person. The full notable-figures listing still includes all biographies and category filters. Its empty notice depends on the ruler selection, not on whether unrelated biographies exist.
- Mohammed Abdu remains at `/ar/people/mohammed-abdu` and `/en/people/mohammed-abdu`, published as `kind=notable` under «الفن» / “Arts” (category ID 1 in the current CMS, article IDs 1–2). Both translations share the category relationship; homepage exclusion is a presentation rule, not unpublishing or moving the article.
- The homepage history overview selects `history/first-saudi-state`, `history/second-saudi-state`, and `history/third-saudi-state` in that order via `saudiStateHistory()`. It uses only available public content; do not fill missing CMS articles with seed data or unrelated history topics.
- `administrativeRegions()` limits the region explorer to the thirteen administrative-region slugs in the standard order and selects only available content. AlUla remains a separate governorate article in `regions`, never a fourteenth administrative region. Article social metadata uses the same public image and localized alt as the article, including safe fallbacks.
- `node scripts/publish-saudi-kings.mjs --validate regions-20260917` checks thirteen bilingual regional articles and generated illustrations offline. `--publish regions-20260917` requires operator authorization and administrator login; it updates only the explicitly listed Riyadh/Asir records and preserves their IDs, URLs, translation keys, media, alt text and editorial associations. Regional noIndex is false for SEO readiness; the site-wide indexing gate remains unchanged. Other batches retain their draft-import preservation rules. The regional batch was published on 2026-09-17 after the operator signed in: Riyadh/Asir article IDs 19–22 were updated with media IDs 10–11 preserved; eleven new region pairs use article IDs 57–78 and media IDs 28–38. Anonymous verification confirmed all 26 regional pages, title/description/canonical/hreflang/social metadata, image delivery, and the 13-link region explorer. The administrative-region filter paginates as 9+4 topics; the existing AlUla article is preserved separately. Total published CMS inventory was 78 language documents. Recheck live records before any rerun; do not recreate existing articles.
- Browser tests require `npm run build` first. `npm run test:e2e` starts the production app on port 3100 with CMS and indexing disabled, and checks both languages without accessing a live database.
- `npm run cms:dev` starts the configured local CMS using the existing Windows-protected configuration. `node scripts/publish-saudi-kings.mjs --validate` checks the bilingual king batch and portraits offline. `--publish` requires explicit editorial authorization and manual administrator sign-in in a fresh Microsoft Edge browser; it uses ordinary REST permissions, preserves existing content, and checks anonymous article/image delivery. Cookie-authenticated automation requests, including `/api/users/me`, must send the configured `Origin`; do not weaken CSRF settings. Never run two publishers concurrently.
- The publisher also accepts `node scripts/publish-saudi-kings.mjs --validate discovery-20260916` (offline) and `--publish discovery-20260916` (explicitly authorized CMS writes after administrator login). This batch contains six original bilingual topics and the economy/nature/tourism sections. Its WebP illustrations are generated from project-authored geometry, not copied source images. Research limitations and conflicting figures are recorded in the batch JSON; never represent unavailable source pages as fully reviewed. This batch was published on 2026-09-16: section IDs 5–7, article IDs 45–56 and media IDs 22–27. Anonymous reads, all twelve article pages, bilingual section search/navigation, illustration captions and narrow-screen layouts were verified. Read current CMS records before rerunning; do not recreate existing documents.
- The user-approved seven-king batch was published to the existing local CMS connection on 2026-09-15: article IDs 31–44 and media IDs 15–21. All fourteen public article pages and seven portraits were verified anonymously in both languages. Read current records before any rerun; do not blindly recreate them. The stored `people` section keeps its slug and is labeled «شخصيات بارزة» / “Notable figures” in the CMS.

